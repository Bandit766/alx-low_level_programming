#include <stdio.h>
#include "main.h"

/**
* _getslen - determines length of input string
* @str: input string
* Return: length including null character
*/
int _getslen(char *str)
{
	int i;

	for(i = 0; str[i] != '\0'; i++)
	{
	}
	return (i);
}
